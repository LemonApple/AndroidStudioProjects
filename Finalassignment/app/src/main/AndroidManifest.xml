<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finalassignment">
    <permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAG" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DepartmentStoreActivity"
            android:exported="true"
            android:theme="@style/AppCompatTheme" /><!--首页：标题栏（搜索和溢出菜单），banner条、推荐商品列表（瀑布流）、底部导航、下拉刷新-->
        <activity android:name=".DepartmentHomeActivity"
            android:theme="@style/AppCompatTheme" />
        <activity android:name=".DepartmentClassActivity"
            android:theme="@style/AppCompatTheme" />
        <activity android:name=".DepartmentCartActivity"
            android:theme="@style/AppCompatTheme" />
        <activity android:name=".SearchResultActvity"
            android:theme="@style/AppCompatTheme">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity android:name=".SearchViewActivity"
            android:exported="true"
            android:theme="@style/AppCompatTheme" />

        <activity android:name=".LaunchImproveActivity"
            android:exported="true" /> <!--App引导页-->

        <activity android:name=".ShoppingCartActivity"
            android:exported="true"></activity><!--购物车列表：加购商品信息、数量、单价、总价、选项菜单、上下文菜单-->
        <activity android:name=".ShoppingDetailActivity"
            android:exported="true"
            android:theme="@style/AppBaseTheme" /><!--商品详情：商品图片、描述、价格等详细信息，选项菜单-->
        <activity android:name=".MainActivity2"
            android:theme="@style/AppBaseTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity><!--商品列表:标题栏（搜索要求从数据库检索信息，并显示结果），商品列表、添加购物车、选项菜单-->
    </application>

</manifest>